import { Route } from '@angular/router';
import { HelloWorldComponent } from './index';

export const HelloWorldRoutes: Route[] = [
  {
    path: 'helloWorld',
    component: HelloWorldComponent
  }
];
